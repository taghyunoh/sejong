<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sejong_helath" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here.
        <item name="android:background">@color/white</item>-->
    </style>
    <style name="splash.Theme.IconBackground" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:background">@color/color_splash_background</item>
    </style>
    <style name="Theme.Sejong_helath" parent="Theme.SplashScreen" >
        <!-- Splash Screen 의 Background 색상.-->

        <item name="windowSplashScreenBackground">@color/spl_back</item>
        <!-- Splash Screen 에서 사용 될 아이콘. 설정 하지 않으면 앱 아이콘 사용. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/icontest_xm</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/color_splash_background</item>
        <!-- Splash Screen 이 끝난 후 보일 화면의 테마-->
        <item name="postSplashScreenTheme">@style/Base.Theme.Sejong_helath</item>

        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>