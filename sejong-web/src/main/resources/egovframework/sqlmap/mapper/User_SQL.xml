<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.sejong.user.mapper.UserMapper">

	<select id="userLoginCheck" parameterType="UserDTO" resultType="UserDTO"><![CDATA[
	  SELECT USER_ID ,
	         USER_ID_NM ,
	  		 USER_NM ,
	         IFNULL(USER_GB,'D')  AS USER_GB,
	         USER_PW,
	         USEYN,
	         IFNULL(DEPT_NM,'') AS DEPT_NM
	    FROM T_ADMIN_MST
	   WHERE USER_ID = #{userId}
	     AND NOW() BETWEEN START_DATE AND END_DATE
	]]>
	</select>

	<!-- 사용자 정보 조회 -->
	<select id="userInfo" parameterType="UserDTO" resultType="UserDTO"><![CDATA[
	  SELECT USER_ID,
	  		 USER_NM,
	  		 USER_ID_NM ,
	         USER_GB,
	         USER_PW,
	         DEPT_NM,
	         DATE_FORMAT(START_DATE,'%y-%m-%d') AS START_DATE,
	         DATE_FORMAT(END_DATE,'%y-%m-%d') AS END_DATE,
	         BIGO,
	         USEYN
	    FROM T_ADMIN_MST
	   WHERE USER_ID = #{userId}
	]]>
	</select>

	<!-- 비밀번호 변경 처리 -->
	<update id="userPwdChange" parameterType="UserDTO"><![CDATA[
	  UPDATE T_ADMIN_MST
	     SET USER_PW  = #{encUserPwd}
	   WHERE USER_ID  = #{userId}
	]]>
	</update>

	<!-- 비밀번호 초기화 처리 -->
	<update id="userPwdReset" parameterType="UserDTO"><![CDATA[
	  UPDATE T_ADMIN_MST
	     SET USER_PW  = #{encUserPwd}
	   WHERE USER_ID  = #{userId}
	]]>
	</update>

	<!-- 약관(개인정보동의/이용약관/고유식별정보) 본문 조회 — T_SIGN_MST -->
	<!-- SEJONG_APP 의 Sign_SQL.xml getSignList 와 동일한 쿼리. SIGN_GB(1/2/3) 매칭, 본문(USE_YN='N')만. -->
	<select id="getSignList" parameterType="map" resultType="SjgnDTO">
		SELECT
		    SIGN_SEQ    AS termsSeq
		   ,SIGN_GB     AS termsGb
		   ,SIGN_NAME   AS termsTitle
		   ,SIGN_CONTS  AS termsConts
		   ,USE_YN      AS useYn
		   ,REG_DTM     AS regDtm
		   ,MOD_DTM     AS modDtm
		FROM T_SIGN_MST
		WHERE SIGN_GB = #{termsGb}
		  AND USE_YN = 'N'
		ORDER BY SIGN_SEQ ASC
	</select>

	<!--
	  termsGb 별 가장 최신 USE_YN='Y' 약관 SEQ 조회.
	  동의이력(T_PERSIGN_TRAN) 저장 시 어느 버전에 동의했는지 기록하기 위해 사용.
	-->
	<select id="selectLatestTermsSeq" parameterType="string" resultType="string">
		SELECT SIGN_SEQ
		  FROM T_SIGN_MST
		 WHERE SIGN_GB = #{termsGb}
		   AND USE_YN  = 'Y'
		 ORDER BY SIGN_SEQ DESC
		 LIMIT 1
	</select>

	<!--
	  동의이력 저장 — T_PERSIGN_TRAN
	  실제 DDL: (PERSIGN_SEQ AUTO_INCREMENT PK, USER_UUID, SIGN_SEQ, AGREE_YN, AGREE_DTM)
	  · #{termsSeq} = 문서 대표 SIGN_SEQ (selectLatestTermsSeq 로 조회한 제목행 USE_YN='Y' 의 SIGN_SEQ)
	  · TERMS_GB/REG_ID/REG_DTM 컬럼은 실제 테이블에 없어 제거함.
	-->
	<insert id="insertPersign" parameterType="PersignDTO">
		INSERT INTO T_PERSIGN_TRAN (
		    USER_UUID
		   ,SIGN_SEQ
		   ,AGREE_YN
		   ,AGREE_DTM
		) VALUES (
		    #{userUuid}
		   ,#{termsSeq}
		   ,IFNULL(#{agreeYn}, 'Y')
		   ,NOW()
		)
	</insert>

</mapper>
