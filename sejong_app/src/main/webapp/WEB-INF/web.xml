<?xml version="1.0" encoding="UTF-8"?>
<web-app id="WebApp_ID" version="3.1" xmlns="http://xmlns.jcp.org/xml/ns/javaee"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd">
	<display-name>egov.sample</display-name>
    
	<filter>
		<filter-name>encodingFilter</filter-name>
		<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>utf-8</param-value>
		</init-param>
	</filter>
	<filter-mapping>
		<filter-name>encodingFilter</filter-name>
		<url-pattern>*.do</url-pattern>
	</filter-mapping>
    
	<filter>
		<filter-name>HTMLTagFilter</filter-name>
		<filter-class>org.egovframe.rte.ptl.mvc.filter.HTMLTagFilter</filter-class>
	</filter>
	<filter-mapping>
		<filter-name>HTMLTagFilter</filter-name>
		<url-pattern>*.do</url-pattern>
	</filter-mapping>
    
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>classpath*:egovframework/spring/context-*.xml</param-value>
	</context-param>
    
	<listener>
		<listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
	</listener>
    
	<servlet>
		<servlet-name>action</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/WEB-INF/config/egovframework/springmvc/dispatcher-servlet.xml</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
    <!-- AI 컨트롤러 -->
	<!--   <servlet>
	    <servlet-name>AiController</servlet-name>
	    <servlet-class>controller.AiController</servlet-class>
	    <load-on-startup>1</load-on-startup>
	  </servlet>
	  <servlet-mapping>
	    <servlet-name>AiController</servlet-name>
	    <url-pattern>/ai/*</url-pattern>
	  </servlet-mapping>
	 -->
    
	<servlet-mapping>
		<servlet-name>action</servlet-name>
		<url-pattern>*.do</url-pattern>
	</servlet-mapping>

	<welcome-file-list>
		<welcome-file>index.jsp</welcome-file>
	</welcome-file-list>
    
	<login-config>
		<auth-method>BASIC</auth-method>
	</login-config>
    
	<!-- 에러 페이지 실제 경로는 /asset/common/error.jsp 다.
	     예전에는 존재하지 않는 /common/error.jsp 를 가리켜서,
	     예외가 나면 톰캣이 그 페이지를 못 찾아 "본문 없는 404" 를 돌려줬다.
	     그래서 500(예: SQL 오류)이 전부 404 로 둔갑해 원인이 가려졌다. -->
	<error-page>
		<exception-type>java.lang.Throwable</exception-type>
		<location>/asset/common/error.jsp</location>
	</error-page>
	<error-page>
		<error-code>404</error-code>
		<location>/asset/common/error.jsp</location>
	</error-page>
	<error-page>
		<error-code>500</error-code>
		<location>/asset/common/error.jsp</location>
	</error-page>

	<!-- 세션 쿠키가 없으면 톰캣이 모든 URL 에 `;jsessionid=...` 를 붙인다(URL 리라이트).
	     그 결과 `/asset/js/app-common.js;jsessionid=...` 같은 정적 파일 요청이 생기고,
	     환경에 따라 404 가 나 화면 스타일이 통째로 깨진다.
	     쿠키로만 세션을 추적하게 해 URL 리라이트를 끈다. -->
	<session-config>
		<!-- 로그인 후 무활동 유지시간(분). 이 시간 방치하면 세션이 만료되고
		     이후 요청은 SessionCheckInterceptor 가 로그인 화면으로 돌려보낸다.
		     (session-timeout 은 XSD 상 tracking-mode 보다 먼저 와야 함) -->
		<session-timeout>60</session-timeout>
		<tracking-mode>COOKIE</tracking-mode>
	</session-config>

	<!-- [PWA] 웹 앱 매니페스트 MIME 타입(Tomcat 기본 미지원) -->
	<mime-mapping>
		<extension>webmanifest</extension>
		<mime-type>application/manifest+json</mime-type>
	</mime-mapping>

</web-app>
